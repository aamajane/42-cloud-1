- name: Update and Upgrade apt packages
  apt:
    update_cache: true
    upgrade: dist
  # ignore_errors: true

- name: Install python 3
  apt:
    name: python3

- name: Install docker
  apt:
    name: docker.io

- name: Install docker-compose
  apt:
    name: docker-compose
